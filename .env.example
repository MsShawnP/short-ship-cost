# Copy to .env and fill in. .env is gitignored; never commit it.
# Local Docker replica or flyctl proxy to the cinderhaven Postgres.
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/cinderhaven
